[global]
   workgroup = {WORKGROUP}
   force user = {USERNAME}
   smb encrypt = {ENCRYPTION}
   server signing = {SIGNING}
   dos charset = cp850
   unix charset = ISO-8859-1
   wide links = yes
   allow insecure wide links = yes

[storage]
   browseable = yes
   writeable = yes
   path = /data
